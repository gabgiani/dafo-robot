diff --git a/gear_sonic/utils/mujoco_sim/base_sim.py b/gear_sonic/utils/mujoco_sim/base_sim.py
--- a/gear_sonic/utils/mujoco_sim/base_sim.py
+++ b/gear_sonic/utils/mujoco_sim/base_sim.py
@@ -63,6 +63,8 @@ class DefaultEnv:

         self.init_scene()
         self.last_reward = 0
+        self.fall = False
+        self.control_started = False

         self.offscreen = offscreen
         if self.offscreen:
@@ -182,6 +184,7 @@ class DefaultEnv:
                 )

         # Enable the elastic band
+        self.elastic_band = None
         if self.config["ENABLE_ELASTIC_BAND"] and self.use_floating_root_link:
             self.elastic_band = ElasticBand()
             if "g1" in self.config["ROBOT_TYPE"]:
@@ -199,8 +202,8 @@ class DefaultEnv:
                     self.mj_model,
                     self.mj_data,
                     key_callback=self.elastic_band.MujuocoKeyCallback,
-                    show_left_ui=False,
-                    show_right_ui=False,
+                    show_left_ui=True,
+                    show_right_ui=True,
                 )
             else:
                 mujoco.mj_forward(self.mj_model, self.mj_data)
@@ -208,7 +211,7 @@ class DefaultEnv:
         else:
             if self.onscreen:
                 self.viewer = mujoco.viewer.launch_passive(
-                    self.mj_model, self.mj_data, show_left_ui=False, show_right_ui=False
+                    self.mj_model, self.mj_data, show_left_ui=True, show_right_ui=True
                 )
             else:
                 mujoco.mj_forward(self.mj_model, self.mj_data)
@@ -247,6 +250,16 @@ class DefaultEnv:
         self.left_hand_index = np.array(self.left_hand_index)
         self.right_hand_index = np.array(self.right_hand_index)

+        self._apply_default_body_pose()
+
+    def _apply_default_body_pose(self):
+        self.mj_data.qpos[self.body_joint_index + self.qpos_offset - 1] = np.asarray(
+            self.robot.DEFAULT_DOF_ANGLES,
+            dtype=float,
+        )
+        self.mj_data.qvel[:] = 0.0
+        mujoco.mj_forward(self.mj_model, self.mj_data)
+
     def init_renderers(self):
         self.renderers = {}
         for camera_name, camera_config in self.camera_configs.items():
@@ -258,6 +271,14 @@ class DefaultEnv:
     def compute_body_torques(self) -> np.ndarray:
         # PD control: tau = tau_ff + kp * (q_des - q) + kd * (dq_des - dq)
         body_torques = np.zeros(self.num_body_dof)
+        if self.unitree_bridge is None or not self.unitree_bridge.low_cmd_received:
+            default_q = np.asarray(self.robot.DEFAULT_DOF_ANGLES, dtype=float)
+            current_q = self.mj_data.qpos[self.body_joint_index + self.qpos_offset - 1]
+            current_dq = self.mj_data.qvel[self.body_joint_index + self.qvel_offset - 1]
+            body_torques = np.asarray(self.robot.MOTOR_KP) * (default_q - current_q) - np.asarray(
+                self.robot.MOTOR_KD
+            ) * current_dq
+            return body_torques
         if self.unitree_bridge is not None and self.unitree_bridge.low_cmd:
             for i in range(self.unitree_bridge.num_body_motor):
                 if self.unitree_bridge.use_sensor:
@@ -353,6 +374,8 @@ class DefaultEnv:
             obs["floating_base_acc"] = self.mj_data.qacc[:6]
         else:
             obs["floating_base_pose"] = np.zeros(7)
+            obs["floating_base_pose"][:3] = self.mj_data.xpos[self.torso_index]
+            obs["floating_base_pose"][3:7] = self.mj_data.xquat[self.torso_index]
             obs["floating_base_vel"] = np.zeros(6)
             obs["floating_base_acc"] = np.zeros(6)

@@ -369,6 +392,9 @@ class DefaultEnv:
             pose[7:10].copy(),
         )
         obs["secondary_imu_vel"] = pose[7:13]
+        if not self.use_floating_root_link:
+            obs["floating_base_vel"][:3] = pose[7:10]
+            obs["floating_base_vel"][3:6] = pose[10:13]

         obs["body_q"] = self.mj_data.qpos[self.body_joint_index + 7 - 1]
         obs["body_dq"] = self.mj_data.qvel[self.body_joint_index + 6 - 1]
@@ -391,6 +417,22 @@ class DefaultEnv:
         self.unitree_bridge.PublishLowState(self.obs)
         if self.unitree_bridge.joystick:
             self.unitree_bridge.PublishWirelessController()
+        if self.fall:
+            return
+        if not self.unitree_bridge.low_cmd_received:
+            return
+        command_targets = np.asarray(
+            [
+                self.unitree_bridge.low_cmd.motor_cmd[index].q
+                for index in range(self.unitree_bridge.num_body_motor)
+            ]
+        )
+        if np.allclose(command_targets, self.robot.DEFAULT_DOF_ANGLES):
+            self.control_started = False
+            return
+        if not self.control_started:
+            self.control_started = True
+            print("SONIC_CONTROL_STARTED", flush=True)
         if self.elastic_band:
             if self.elastic_band.enable and self.use_floating_root_link:
                 pose = np.concatenate(
@@ -506,13 +548,10 @@ class DefaultEnv:
             self.apply_perturbation(key)

     def check_fall(self):
-        self.fall = False
         if self.mj_data.qpos[2] < 0.2:
-            self.fall = True
-            print(f"Warning: Robot has fallen, height: {self.mj_data.qpos[2]:.3f} m")
-
-        if self.fall:
-            self.reset()
+            if not self.fall:
+                self.fall = True
+                print(f"Warning: Robot has fallen, height: {self.mj_data.qpos[2]:.3f} m")

     def check_self_collision(self):
         robot_bodies = get_subtree_body_names(self.mj_model, self.mj_model.body(self.root_body).id)
@@ -524,7 +563,12 @@ class DefaultEnv:
         return self_collision

     def reset(self):
+        self.fall = False
+        self.control_started = False
+        if self.unitree_bridge is not None:
+            self.unitree_bridge.reset()
         mujoco.mj_resetData(self.mj_model, self.mj_data)
+        self._apply_default_body_pose()


 class BaseSimulator:
@@ -574,6 +618,7 @@ class BaseSimulator:
         self.init_publisher()

         self.sim_thread = None
+        print("SONIC_SIM_READY", flush=True)

     def start_as_thread(self):
         self.sim_thread = Thread(target=self.start)
diff --git a/gear_sonic/utils/mujoco_sim/unitree_sdk2py_bridge.py b/gear_sonic/utils/mujoco_sim/unitree_sdk2py_bridge.py
--- a/gear_sonic/utils/mujoco_sim/unitree_sdk2py_bridge.py
+++ b/gear_sonic/utils/mujoco_sim/unitree_sdk2py_bridge.py
@@ -86,6 +86,10 @@ class UnitreeSdk2Bridge:
         self.right_hand_state_puber = ChannelPublisher("rt/dex3/right/state", HandState_)
         self.right_hand_state_puber.Init()

+        self.low_cmd_lock = threading.Lock()
+        self.left_hand_cmd_lock = threading.Lock()
+        self.right_hand_cmd_lock = threading.Lock()
+
         self.low_cmd_suber = ChannelSubscriber("rt/lowcmd", LowCmd_)
         self.low_cmd_suber.Init(self.LowCmdHandler, 1)

@@ -96,10 +100,6 @@ class UnitreeSdk2Bridge:
         self.right_hand_cmd_suber = ChannelSubscriber("rt/dex3/right/cmd", HandCmd_)
         self.right_hand_cmd_suber.Init(self.RightHandCmdHandler, 1)

-        self.low_cmd_lock = threading.Lock()
-        self.left_hand_cmd_lock = threading.Lock()
-        self.right_hand_cmd_lock = threading.Lock()
-
         self.wireless_controller = unitree_go_msg_dds__WirelessController_()
         self.wireless_controller_puber = ChannelPublisher(
             "rt/wirelesscontroller", WirelessController_
diff --git a/gear_sonic/utils/mujoco_sim/wbc_configs/g1_29dof_sonic_model12.yaml b/gear_sonic/utils/mujoco_sim/wbc_configs/g1_29dof_sonic_model12.yaml
--- a/gear_sonic/utils/mujoco_sim/wbc_configs/g1_29dof_sonic_model12.yaml
+++ b/gear_sonic/utils/mujoco_sim/wbc_configs/g1_29dof_sonic_model12.yaml
@@ -17,7 +17,7 @@ JOYSTICK_DEVICE: 0 # Joystick number
 FREE_BASE: False

 PRINT_SCENE_INFORMATION: True # Print link, joint and sensors information of robot
-ENABLE_ELASTIC_BAND: True # Virtual spring band, used for lifting h1
+ENABLE_ELASTIC_BAND: False # Virtual spring band, used for lifting h1

 SIMULATE_DT: 0.005  # Need to be larger than the runtime of viewer.sync()
 VIEWER_DT: 0.02  # Viewer update time
@@ -154,32 +154,32 @@ NUM_HAND_JOINTS: 7
 NUM_UPPER_BODY_JOINTS: 17

 DEFAULT_DOF_ANGLES: [
-    -0.1,  # left_hip_pitch_joint 
+  -0.312,  # left_hip_pitch_joint
     0.0,  # left_hip_roll_joint
     0.0,  # left_hip_yaw_joint
-    0.3,  # left_knee_joint
-    -0.2, # left_ankle_pitch_joint
+  0.669,  # left_knee_joint
+  -0.363, # left_ankle_pitch_joint
     0.0,  # left_ankle_roll_joint
-    -0.1, # right_hip_pitch_joint
+  -0.312, # right_hip_pitch_joint
     0.0,  # right_hip_roll_joint
     0.0,  # right_hip_yaw_joint
-    0.3,  # right_knee_joint
-    -0.2, # right_ankle_pitch_joint
+  0.669,  # right_knee_joint
+  -0.363, # right_ankle_pitch_joint
     0.0,  # right_ankle_roll_joint
     0.0,  # waist_yaw_joint
     0.0,  # waist_roll_joint
     0.0,  # waist_pitch_joint
-    0.0,  # left_shoulder_pitch_joint
-    0.0,  # left_shoulder_roll_joint
+  0.2,  # left_shoulder_pitch_joint
+  0.2,  # left_shoulder_roll_joint
     0.0,  # left_shoulder_yaw_joint
-    0.0,  # left_elbow_joint
+  0.6,  # left_elbow_joint
     0.0,  # left_wrist_roll_joint
     0.0,  # left_wrist_pitch_joint
     0.0,  # left_wrist_yaw_joint
-    0.0,  # right_shoulder_pitch_joint
-    0.0,  # right_shoulder_roll_joint
+  0.2,  # right_shoulder_pitch_joint
+  -0.2,  # right_shoulder_roll_joint
     0.0,  # right_shoulder_yaw_joint
-    0.0,  # right_elbow_joint
+  0.6,  # right_elbow_joint
     0.0,  # right_wrist_roll_joint
     0.0,  # right_wrist_pitch_joint
     0.0   # right_wrist_yaw_joint
